 
//import {} from '..';
 
import {groupBy} from '@std/fp';
const log = console.log

describe('', () => {
    test('', async () => {
        const res = groupBy //await ();
        log('', res);
        expect(res).toBeDefined();
    });
})